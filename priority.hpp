#ifndef PRIORITY_HPP
#define PRIORITY_HPP

#include <vector>
#include "process.hpp"

// Preemptive Priority Scheduling (lower priority value = higher priority)
void runPreemptivePriorityScheduling(std::vector<Process> &allProcesses);

#endif
